spring:
  application:
    name: hcc
  profiles:
    active: test
  config:
    import: optional:configserver:http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: "*"
